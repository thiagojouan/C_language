#include <stdio.h>

int main() 
{

int age;

printf ("How old are you: ");
scanf ("%d", &age);
                            
if (age <= 8) {
    printf ("Wow, pleople starting using computer early");
} 
return 0;
}