#include<stdio.h>
int main(){

    int values [] = {32,12,54,78,39,77};

    for (int i = 0; i < 6; i++)
    {
        printf ("The value in the position %d is: %d\n", i,values[i]);
    }
    
return 0;

}